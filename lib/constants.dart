import 'package:flutter/material.dart';

const  String appBarTitle = 'Currencies';

final TextStyle mainStyle =
    TextStyle(color: Colors.blue, fontSize: 32, fontWeight: FontWeight.bold);

const padding_5 = EdgeInsets.symmetric(horizontal: 5);

const padding_15 = EdgeInsets.symmetric(horizontal: 15);

const bgColor = Colors.amber;
const colorUP = Colors.green;
const colorDOWN = Colors.red;

const BoxShape circular = BoxShape.circle;

const FontWeight bold = FontWeight.bold;
